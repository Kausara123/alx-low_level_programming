#include "main.h"

/**
 *
 * main - check the code
 * Return: Always 0.
 */
void print_alphabet(void)
{
	int letter;
	for(letter='a'; letter<='z'; letter++)
	{
		putchar(letter);
	        return (0);
	}
}
